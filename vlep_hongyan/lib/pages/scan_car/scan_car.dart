import 'package:flutter/material.dart';

class ScanCarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('扫描车辆'),
      ),
    );
  }
}